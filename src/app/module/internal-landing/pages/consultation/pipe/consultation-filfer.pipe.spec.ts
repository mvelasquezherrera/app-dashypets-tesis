import { ConsultationFilferPipe } from './consultation-filfer.pipe';

describe('ConsultationFilferPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultationFilferPipe();
    expect(pipe).toBeTruthy();
  });
});
